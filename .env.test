# Aponta para um banco separado do de desenvolvimento — os testes de
# integração apagam dados entre cada teste, então não pode ser o mesmo
# banco que você usa manualmente no Apollo Sandbox.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/controle_apostas_test"
